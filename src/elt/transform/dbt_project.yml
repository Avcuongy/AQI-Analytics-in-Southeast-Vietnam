name: "dbt_transform"
version: "1.0.0"
profile: "profiles"

log-path: ["../../../logs"]
model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  dbt_transform:
    staging:
      +materialized: view
      +schema: staging

    marts:
      +materialized: table
      +schema: marts

vars:
  staging_path: "../../../data/staging"
