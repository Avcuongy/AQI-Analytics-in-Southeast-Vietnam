# src/elt/transform/dbt_project.yml
name: "dbt_transform"
version: "1.0.0"
profile: "transform"

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  dbt_transform:
    staging:
      +materialized: view
      +schema: staging

    marts:
      +materialized: table
      +schema: marts

vars:
  staging_path: "../../../data/staging"
